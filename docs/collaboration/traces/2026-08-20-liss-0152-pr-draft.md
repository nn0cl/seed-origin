# Trace: LISS-0152 PR draft (merge prep only — do not push)

- Date: 2026-08-20
- Path: Feature Path (documentation only; no `gh pr create`, no push)
- Issue: LISS-0152
- Branch: `feature/liss-0152-client-side-prediction`
- Worktree: `/Users/nn0cl/Documents/git/seed-origin-prediction`
- Base: `main` @ `932087c0dd6b998abc0bb2b7b9be8523eea447c3`
- HEAD: `040789569f6d07e51c950de02aa062062d28c805` (23 commits ahead of main)

## Merge readiness

| Check | Result |
| --- | --- |
| Working tree clean | yes |
| Commits vs main | 23 (`git log main..HEAD --oneline`) |
| Diff size | 111 files, +7564 / −272 |
| CI equivalent (`./build/seed_tests`) | **EXIT 0** (2026-08-20) |
| LISS-0152 status | `done` / Phase 3 refactor complete |
| Auth worktree isolation | no seed-auth or `/Users/nn0cl/Documents/git/seed-origin` auth edits on this branch |
| LISS-0155 worktree | not mixed |
| Merge blockers | **none identified** for CSP core; LISS-0153 durable-policy Adjudicator items remain open but are non-blocking for LISS-0152 merge |

## Commit range (newest first)

```text
0407895 Mark LISS-0152 done after Adjudicator Phase 3 approval.
ac4e200 Refactor CSP codec and response enqueue paths without behavior change.
573d705 Document LISS-0152 completion handoff and move issue to review.
e3a9fde Close TCP after an Accepted Disconnect ack is flushed.
89f9587 Ack Disconnect before resetting client auth, and keep TCP open.
95d707e Reset client auth after Disconnect and verify TCP lifecycle unbind.
4474e6b Implement Disconnect to end sessions and omit public poses.
bad6ecb Treat Disconnect as the unimplemented command now that Move is handled.
e4a49bc Treat the first Event sequence jump as a gap so clients request a Snapshot.
53f8b6a Resolve combat and spell HP from Player or NPC so NPC targets do not null-deref.
dd6abfe Charge spell MP with an IEEE-exact tenth so power 50 costs 5.
154a833 Restore the open-decisions heading in the operator-rename work trace.
6b44ee1 Release previous PlayerNames when operators assign a new unique name.
e41b2e5 Keep trimmed PlayerNames claimed after logout and Field unset.
b6baf02 Require operator-assigned names and keep identity ids stable on reconnect.
ee51d5a Key remotes by gameplay id so reconnect does not duplicate other players.
1fde97f Place login spawn with configurable status and split identity roles.
b6df371 Parse CastSpell element on commas so the accepted payload reaches the input queue.
60758fe Put RequestSnapshot on POSIX TCP after reconnect so recovery can fetch a Snapshot.
d0df6d3 Wire RequestSnapshot so gap and reconnect clients can fetch a full Snapshot.
3693705 Place logged-in sessions on Field so join Snapshots list public poses.
7682215 docs(spec): record full vs delta WorldUpdate on one sequence.
0707eef Publish public player poses on join Snapshots so idle remotes appear without a guessed broadcast Hz.
```

## PR draft (for Adjudicator — create only after explicit push approval)

**Title:** LISS-0152: client-side prediction, reconciliation, and reconnect contract

**Summary**

- Local-player client-side prediction with sequenced Move input, server
  owner-only movement ack, pending-input replay, and prediction/render
  separation (`LocalMovementPredictor`, 150 ms smooth / 5.0 snap).
- Full Snapshot vs delta Event on one WorldUpdate sequence; sequence gaps
  trigger RequestSnapshot (LISS-0154 wire Command type 7 on this branch).
- Disconnect lifecycle (Accepted ack → auth reset → TCP close), reconnect I/O
  (Login → RequestSnapshot → coalesced Snapshot), and early LISS-0153 identity
  subset (four roles, operator PlayerName, claimed-name registry, gameplay-id
  remote tracking).
- Phase 3 refactor: shared `FrameHeaderBinary`, `ServerRuntime` response
  enqueue helpers, `ClientTransportShell` inbound handler split — behavior
  unchanged, full suite green.

**Test plan**

- [ ] `./build/seed_tests` — full suite EXIT 0
- [ ] Spot-check `ClientPredictionSyncTest`, `ClientTransportShellTest`
  (loopback disconnect/reconnect + RequestSnapshot), `DisconnectCommandHandlerTest`
- [ ] Confirm no seed-auth or LISS-0155 files in diff
- [ ] Adjudicator review of LISS-0153 open decisions before treating early
  spawn subset as durable policy

**Out of scope (explicit)**

- seed-auth implementation
- PlayerName case/Unicode fold, zone spawn, auth-side name ledger
- Renderer / LISS-0064
- Merge/push without Adjudicator confirmation

## Blockers for merge

None for LISS-0152 acceptance. Follow-on work (LISS-0153 Adjudicator
decisions, LISS-0128 timeout/ops) can land on the same branch or a successor
branch after push/PR approval.

## Next safe action (Adjudicator)

1. Review this draft and the completion handoff trace
   (`2026-08-20-liss-0152-completion-handoff.md`).
2. If satisfied, explicitly approve **push** and PR creation.
3. Select LISS-0153 durable-policy answers (see updated
   `docs/issues/LISS-0153-login-field-spawn-policy.md`).
